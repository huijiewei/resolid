---
title: 介绍
---

import { mergeFrontmatter } from "@resolid/remix-utils";

export const meta = mergeFrontmatter(frontmatter);

Resolid React UI 是一个用于使用 React 构建可访问的 Web 应用程序和设计系统的 UI 工具包。它提供了一组低级 UI 组件和基元，可以成为您的设计系统实现的基础。

## 原则

### 简单

旨在提供极简主义 API，以便您可以快速构建所需的内容。

### 可访问的

### 一致的
